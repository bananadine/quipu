# Active region - one of: us, eu, uk
# Switching this value re-targets every test (API + Web) to a different region's
# base URLs, test data, currency, and locale - no test code changes required.
REGION=us

# Optional: override Playwright defaults
# CI=true
# HEADED=false
# WORKERS=4
